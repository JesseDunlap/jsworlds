module.exports = {
	// The amount of rounds to run for salt generation.
	// A higher number is slower, but more secure.
	// General default/recommended value is 10
	saltRounds: 				10,
	
	defaultPermissions: 		["user", "admin"]
};