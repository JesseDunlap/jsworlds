/**
 * Configuration for MongoDb connections
 * (Configuration can be overwritten later manually)
 */
module.exports = {
	host: 			"127.0.0.1",
	port: 			27017,
	
	// The following two values can be left empty if the target server
	// does not require authentication.
	username: 		"",
	password: 		"",
	
	database: 		"P"
};